/* ============================================================
   Where is the agent server?

   Leave empty ("") when server.js serves this page itself —
   e.g. running `npm start` and opening http://localhost:3000.

   Set it when this page is hosted somewhere else (like GitHub
   Pages) and the agent runs on its own server, e.g.:
     window.TRACKBOT_API = "https://shawnpc.your-tailnet.ts.net";

   If the server can't be reached, the page still works in
   rule-based mode, so the demo never breaks.
   ============================================================ */
window.TRACKBOT_API = "https://trackbot-3w10.onrender.com";
